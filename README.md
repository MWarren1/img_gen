# img_gen

```
usage: img_gen [-h] [--width WIDTH] [--height HEIGHT] [--output OUTPUT]

Randomly generates image

options:
  -h, --help       show this help message and exit
  --width WIDTH    Width of image
  --height HEIGHT  Height of image
  --output OUTPUT  filename of the output image
```